辉度图		irradiance cube	64像素，32*4
输入一个方向( N 世界坐标近似 )	半球卷积采样  方位角+天顶角 (360度/180+90度/64)   卷积采样 cube

预过滤贴图 	prefilterEnv	512像素
粗糙度 		对应 mipmap 的等级	采样数 32

离屏 渲染  帧大小=64,512
对每一级别的mip，cube的每一个面 都来一次


brdf lut		brdf look-up-texture		512像素
离屏渲染的 frame-buffer 就是 这个texture


在 Scene.h 中
加载 pbr 的环境数据  		 辉度图	Lut	预过滤贴图
单个物体的渲染数据		gltfPbrRender	纹理+法线+OC+自发光+金属度/高光	曝光 调试参数

高光流			纹理+高光		在 gltf 材质的 extension中

MSAA			硬件Feature	samplerAnisotropy		sampleRateShading ( 非几何边缘 的 内部高频锯齿 )

MSAA (用 Subpass 来做 )	片上缓存 		额外的两个 渲染目标可以设定为 VK_IMAGE_USAGE_TRANSIENT_ATTACHMENT_BIT
第一个 Color Attachment	渲染目标 		(可用片上高速缓存代替)
第二个 Color Attachment	渲染目标		将第一个 Color Attachment 的 resolve 目标
Depth 照搬 Color的流程


MSAA 是 RenderPass 层级	
影响到	RenderPass 中	所有执行的 GraphicsPipeline ( 需要设定状态 MultiSample StateCreateInfo 的  rasterizationSamples )
如果某个	Pipeline	未设定正确的  MultiSample  图像位置就会出现 奇怪的漂移 和 遮挡关系

